---
title: "API概要"
description: "ShanoneのREST APIサーフェス — 同じToolベンダー・Skillベンダー・権限ベンダーの機能を、MCPを使わずに素のHTTPで公開したもの。"
---

## Shanoneにプログラムからアクセスする2つの方法

ほとんどのエージェント連携には[MCPサーバー](/ja/mcp/overview)を使うべきです——Cursor、Claude Code、その他のMCPクライアントはこれをネイティブに話します。ここで解説するREST APIは、その裏側にある機能そのものであり、MCPクライアントを介さずにスクリプト・バックエンドサービス・CIジョブから直接Shanoneを呼び出したい場合に便利です。実際、Shanone自身のローカルstdio MCPプロキシ(`@duzzle/shanone-mcp`とそのPython版)も、このREST APIをごく薄くラップしただけの実装になっています。

## ベースURL

```
https://app.shanone.ai/api
```

## 認証

外部からのAPIリクエスト(WebダッシュボードのセッションベースのJWT認証とは異なります)は、ShanoneのAPIキーを使って認証します。

```bash
curl "https://app.shanone.ai/api/v1/tools/search?query=post+slack+message" \
  -H "Authorization: Bearer sh_xxxxxxxxxxxxxxxx"
```

キーの管理、スコープ設定、レート制限については[認証](/ja/api-reference/authentication)を参照してください。

## エンドポイントのグループ

<CardGroup cols={2}>
  <Card title="Toolベンダー" icon="wrench">
    `GET /v1/tools/search`、`GET /v1/tools/list`、`GET /v1/tools/{tool_name}/schema`、`POST /v1/tools/{tool_name}/execute`
  </Card>
  <Card title="Skillベンダー" icon="bookmark">
    `GET /v1/skills`、`GET /v1/skills/{skill_id}`、`POST /v1/skills`、`PUT /v1/skills/{skill_id}`、`DELETE /v1/skills/{skill_id}`
  </Card>
  <Card title="権限ベンダー" icon="shield-check">
    `GET /v1/permissions/services`、`GET /v1/permissions/users/{user_id}`、`PATCH /v1/permissions/users/{user_id}/tools/{tool_name}`、`PATCH /v1/permissions/users/{user_id}/services/{service_name}`、`GET /v1/permissions/users/{user_id}/summary`、`PATCH /v1/permissions/users/{user_id}/batch`
  </Card>
  <Card title="ヘルスチェック" icon="heart-pulse">
    `GET /v1/health` — `shanone_health_check`の裏側にある、同じチェック
  </Card>
</CardGroup>

これらは同じ形をしたMCPメタツールに直接対応しています——`GET /v1/tools/search`は`shanone_search_tools`が内部で呼び出しているエンドポイントであり、`POST /v1/tools/{tool_name}/execute`は`shanone_execute_tool`に対応します。すでに使いたいMCPツールがわかっている場合、REST側のエンドポイントもほぼ同じ動詞を、少し形の違うURLに向けて使うだけです。

<Note>
Shanoneのダッシュボード(Settings、API Keys、Webhooks、Integrationsなど)は、はるかに大規模なJWT認証の内部ルート群によって支えられています——これらは外部APIの一部ではなく、APIキーで直接呼び出すことを意図したものではありません。
</Note>

## 例: 検索してから実行する

<Tabs>
  <Tab title="cURL">
    ```bash
    # 1. 検索
    curl "https://app.shanone.ai/api/v1/tools/search?query=post+slack+message&limit=5" \
      -H "Authorization: Bearer sh_xxxxxxxxxxxxxxxx"

    # 2. 実行
    curl -X POST "https://app.shanone.ai/api/v1/tools/slack_post_message/execute" \
      -H "Authorization: Bearer sh_xxxxxxxxxxxxxxxx" \
      -H "Content-Type: application/json" \
      -d '{"arguments": {"channel": "#general", "text": "Hello from the API!"}}'
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import requests

    headers = {"Authorization": "Bearer sh_xxxxxxxxxxxxxxxx"}
    base = "https://app.shanone.ai/api"

    search = requests.get(f"{base}/v1/tools/search",
                          params={"query": "post slack message", "limit": 5},
                          headers=headers).json()

    result = requests.post(f"{base}/v1/tools/slack_post_message/execute",
                            json={"arguments": {"channel": "#general", "text": "Hello from the API!"}},
                            headers=headers).json()
    ```
  </Tab>
</Tabs>

## レスポンスの形式

ToolベンダーとSkillベンダーのエンドポイントは、(MCPツールが生成するMarkdown形式のテキストではなく)素のJSONオブジェクトを返します。例えば`shanone_search_tools`の裏側にあるエンドポイントは、次のような値を返します。

```json
{
  "tools": [{ "name": "slack_post_message", "service": "slack", "description": "Post a message to a Slack channel or user." }],
  "total": 1,
  "query": "post slack message",
  "offset": 0,
  "limit": 5,
  "has_more": false
}
```

実行結果は、MCP経由で`shanone_execute_tool`を呼んだ場合でもREST経由で`POST /v1/tools/{tool_name}/execute`を呼んだ場合でも、[Toolsリファレンス](/ja/mcp/tools-reference#error-responses)で説明されている同じ`success` / `reason` / `connect_link`の形式に従います。

## レート制限

| 設定項目 | デフォルト値 |
|---------|---------|
| 1分あたりのリクエスト数 | 60 |
| 1日あたりのリクエスト数 | 10,000 |

いずれもAPIキーごとに**Settings → API Keys**から設定変更でき(最大1,000/分、1,000,000/日まで)、どちらかを超えると`Retry-After`ヘッダー付きで`429`が返されます。

## 次のステップ

<CardGroup cols={2}>
  <Card title="認証" icon="key" href="/ja/api-reference/authentication">
    APIキーの形式、スコープ設定、IP許可リスト、ローテーション
  </Card>
  <Card title="MCP Toolsリファレンス" icon="book" href="/ja/mcp/tools-reference">
    同じ機能を、MCPのツール呼び出し側から見た説明
  </Card>
</CardGroup>
